{
  "name": "my-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^8.1.6"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:api": "lerna run start --scope api",
    "build:widget": "lerna run build --scope widget"
  }
}
